# Apps

Logical application grouping for the dashboard-first control plane.

- `onyx/` documents the runtime module entry.
- `mystarterkit/` documents the governance and evidence engine entry.

The source of truth remains in `upstream/onyx` and `overlays/myStarterKit`.
