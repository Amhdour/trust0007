# OPA Envoy Integration Stub

This folder contains local-development OPA policy examples used by Envoy `ext_authz`.

- `policy.rego`: minimal allow/deny policy package at `envoy.authz`.

For local development only; do not treat as production-grade authorization policy.
