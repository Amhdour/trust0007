# Evidence

Dashboard-owned evidence exports, screenshots, and generated packs can be collected here over time.

Current source artifacts still live under `overlays/myStarterKit/artifacts/` and `telemetry/`.

Reviewer-facing live proofs are indexed under `evidence/reviewer/inspectable-live-runtime/`.
